@model C4FAMS.Models.ThanhTuuViewModel

<div class="container">
    <h1 class="mt-4 mb-4" style="background: linear-gradient(90deg, #00BCD4, #0288D1); color: white; padding: 10px; border-radius: 5px;">Chỉnh sửa Thành tựu</h1>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            @TempData["Error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <form asp-action="EditAchievement" method="post" class="shadow-sm rounded p-3">
        <div asp-validation-summary="All" class="text-danger"></div>
        <input type="hidden" asp-for="MaThanhTuu" />

        <div class="row g-3">
            <div class="col-md-12">
                <label asp-for="TenThanhTuu" class="form-label fw-bold fs-6 text-cyan"></label>
                <input asp-for="TenThanhTuu" class="form-control fs-6" placeholder="Nhập tên thành tựu" required />
                <span asp-validation-for="TenThanhTuu" class="text-danger"></span>
            </div>

            <div class="col-md-12">
                <label asp-for="MoTa" class="form-label fw-bold fs-6 text-cyan"></label>
                <input asp-for="MoTa" class="form-control fs-6" placeholder="Nhập mô tả" />
                <span asp-validation-for="MoTa" class="text-danger"></span>
            </div>

            <div class="col-md-6">
                <label asp-for="NamDatDuoc" class="form-label fw-bold fs-6 text-cyan"></label>
                <input asp-for="NamDatDuoc" class="form-control fs-6" type="number" placeholder="Nhập năm đạt được" required />
                <span asp-validation-for="NamDatDuoc" class="text-danger"></span>
            </div>

            <div class="col-md-12 mt-4">
                <button type="submit" class="btn btn-primary">Lưu</button>
                <a asp-action="ManageProfile" class="btn btn-secondary ms-2">Quay lại</a>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

<style>
    .text-cyan { color: #00BCD4; }
    .fs-6 { font-size: 0.95rem; }
</style>